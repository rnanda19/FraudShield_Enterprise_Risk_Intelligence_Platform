# Python
__pycache__/
*.pyc
.ipynb_checkpoints/
*.egg-info/
.venv/
venv/

# OS
.DS_Store
Thumbs.db

# Notebook-run scratch output (regenerated by running the notebook — the
# committed copy under reports/nb1_results/ is the real, already-executed one)
notebooks/nb1_results/

# NOTE: data/raw/creditcard.csv is intentionally NOT ignored — the raw dataset
# is tracked in this repo. See README.md's Git LFS note before pushing.
